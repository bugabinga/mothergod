{"id": "it42", "date": "2026-08-22", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) exists yet in-repo, so no experiment this session can measure a bits/byte delta; the smallest useful slice is porting the two mandatory corpus generators (POLICY.md) from the founding session's corpus.py into a new Rust bench/ crate, unblocking every future harness-dependent experiment.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "new: bench/ generators (entropy_ladder H in {1,2,4,6,8}, markov_h8_2_trap), 200000-byte samples, seed 0xC0FFEE123456789A", "mechanism": "geometric-weight bisection (behavior-ported from _skewed_weights) plus an additive random walk on Z/256 reproduce the founding session's mandatory datasets; measured order-0 entropy landed within 0.004 bits of each ladder target, and the markov trap measured h0=7.998 (target 8.0) with h1=1.987 (target 2.0), confirming the histogram/context-model separation JOURNAL S1-L3 requires. No bpb deltas apply: there is no codec beyond the Stored method yet, so there is no champion to diff against.", "commit": null}
{"id": "it43", "date": "2026-08-22", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the smallest useful M1 slice is porting the fixed-stride delta filter (JOURNAL S1-A2) into src/filters.rs as a standalone reversible transform, unblocking the parse/models/coder modules that will eventually sit behind it.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported sdelta/usdelta behavior (wrapping difference/sum against the byte `stride` positions back) with stride typed as NonZeroUsize so a zero stride, which would zero every byte instead of transforming it, is unrepresentable rather than a runtime check; 6 unit tests cover round-trip across 10 strides between 1 and 1001, empty/short/single-byte inputs, and u8 wraparound. Exposed as `pub mod filters` (not pub(crate)) so the module stays reachable under `--deny warnings` (dead_code) while unwired to a Method variant.", "commit": null}
{"id": "it44", "date": "2026-08-22", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the smallest useful M1 slice is porting the row-major-to-column-major transpose filter (JOURNAL S1-A2) into src/filters.rs alongside the delta filter, unblocking the parse/models/coder modules that will eventually sit behind both.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported tpose/untpose behavior (rewrite data, interpreted as rows of columns bytes, column by column) with columns typed as NonZeroUsize so a zero column count, which has no rows to transpose, is unrepresentable rather than a runtime check; 6 unit tests cover round-trip across 10 column counts between 1 and 1001, empty/short/single-byte inputs, an explicit grouping check, and single-column identity. filters.rs split its flat encode/decode into delta and transpose submodules so the two filter kinds' functions of the same name don't collide.", "commit": null}
{"id": "it45", "date": "2026-08-22", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the smallest useful M1 slice is porting the x86 call/jmp (BCJ) filter (JOURNAL S1-A2) into src/filters.rs alongside delta and transpose, unblocking the parse/models/coder modules that will eventually sit behind all three.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported bcj(d, enc) behavior (rewrite the 4-byte little-endian operand following every 0xE8/0xE9 opcode between a position-relative offset and an absolute one) as two functions, encode/decode, instead of one function with a boolean flag, matching this module's established shape; cross-checked against research/imports/session-1/research_state.json's .filters.bcj (the founding session's Python source), which matches mothergod.rs's bcj exactly. 8 unit tests cover round-trip on empty/too-short-for-an-operand inputs, explicit E8 and E9 operand rewrites, identity when no opcode byte is present, round-trip over 2000 bytes of cyclic data that does contain opcode bytes, 20 adjacent instructions, and a wrapping-overflow operand. Also identified: base64-unwrap and reverse (remaining S2-D2 filter kinds) are absent from mothergod.rs but present in research_state.json's .filters.b64/.filters.rev, the correct source for their future ports.", "commit": null}
{"id": "it46", "date": "2026-08-23", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the smallest useful M1 slice is porting the base64-unwrap filter (JOURNAL S1-A2, the founding session's single biggest ratio drop) into src/filters.rs alongside delta, transpose, and bcj, unblocking the parse/models/coder modules that will eventually sit behind all four.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported filt/unfilt behavior (research_state.json .filters.b64): scan a length/alphabet-eligible buffer, strict-decode it, and accept only if re-encoding reproduces the exact input bytes (catches non-canonical padding bits), prefixing a one-byte flag so decode knows whether to unwrap or pass through -- a data-dependent decision, unlike the parameter-driven delta/transpose/bcj filters. Wrote a zero-dependency standard base64 encode/strict-decode pair to support it (no base64 crate per ADR-0002). 10 unit tests cover round-trip on empty/too-short inputs, valid unwrap, non-base64 pass-through, invalid padding placement, non-canonical padding bits (explicit guard-exercise assertion), all three padding-length classes, empty decode, and a 300-byte binary payload round-trip.", "commit": null}
{"id": "it47", "date": "2026-08-23", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the smallest useful M1 slice is porting the byte-order reversal filter (JOURNAL S1-A2, right-anchored structure) into src/filters.rs alongside delta, transpose, bcj, and base64-unwrap, completing the filter-bank checklist and unblocking the parse/models/coder modules that will eventually sit behind all five.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported filt/unfilt behavior (research_state.json .filters.rev, both d[::-1]) as reverse::encode/decode: the filter is its own inverse, so decode is encode under a different name, kept as two functions to match the other four filters' encode/decode-pair shape. 6 unit tests cover round-trip on empty/single-byte inputs, an explicit byte-order check, an explicit encode-twice-is-identity check, round-trip across 8 lengths between 0 and 1000, and a palindrome fixed point.", "commit": null}
{"id": "it48", "date": "2026-08-23", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the smallest useful M1 slice is porting the pick_filters trial-selection heuristic (JOURNAL S1-A1) into src/filters.rs as a select submodule, unblocking the eventual trial-encode loop that will call it alongside the LZ/model/coder modules still to come.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported pick_filters behavior (mothergod.rs): an order-1 entropy proxy on a bounded 16384-byte probe scores identity against 96 delta strides, keeping the best plus identity; a full-input opcode-density scan (E8/E9 hits over 1 in 400 bytes) shortlists BCJ; a column-entropy proxy over 14 candidate column counts shortlists transpose when it beats the untransposed baseline by 0.35 bits/byte, only above a 4096-byte minimum. Deviated from the archive's raw u8 candidate ids (0/1..=96/97/100+i, a single overloaded integer space) to a Candidate enum (Identity/Delta(NonZeroUsize)/Bcj/Transpose(NonZeroUsize)) so a delta stride and a transpose column count cannot be confused; base64_unwrap and reverse stay out of the shortlist because pick_filters itself never covers them in the archive (same basis as S2-A5/S2-A6). 6 unit tests cover identity always present, a synthetic small-random-walk fixture where the correct delta stride wins, opcode-dense vs sparse BCJ shortlisting, a column-structured fixture that shortlists transpose, and transpose never firing below the minimum length.", "commit": null}
{"id": "it49", "date": "2026-08-23", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the smallest useful M1 slice after the filter bank is complete (S2-A2 through S2-A7) is porting the archive's greedy/lazy LZ parser (lz, JOURNAL S1-A3) into a new src/lz.rs, ahead of lz_opt since lz_opt prices candidates against entropy-model frequency tables that don't exist yet and runs lz internally as its own price-seeding first pass.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported lz's behavior (hash-chain match finder over a 3-byte prefix hash, one-step lazy matching, 3-slot repeat-offset cache) as Token/parse_greedy, plus a replay function (parse_greedy's inverse) that proves round-trip correctness at the token level without needing the entropy coder to exist. Deviated from the archive's shared find closure and raw (usize, usize) (0,0) no-match sentinel: two named functions (match_len, MatchFinder::find_best) and an Option<(usize, Distance)> return, Distance a NonZeroU32 newtype, close the exact rep-symbol/offset-bucket confusion class the session-1 port bug came from. 11 unit tests cover empty/single-byte/no-repeat inputs, a simple repeat, an overlapping run-length fixture, a run spanning multiple MAX_MATCH_LEN-bounded tokens, an alternating pattern exercising the rep cache, cyclic data, a structured fixture exercising the lazy-matching check at a non-initial distance, and a fixture with zero bytes present.", "commit": null}
{"id": "it50", "date": "2026-08-23", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the next M1 slice after the greedy/lazy parser (S2-A8) is porting the archive's DP-priced optimal parse (lz_opt, JOURNAL S1-A3) into src/lz.rs as parse_optimal, since it runs the greedy parser internally as its own price-seeding first pass and prices candidates against its own lightweight frequency tables, not the entropy models this crate doesn't have yet.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported lz_opt's two-round DP (a parse_greedy-seeded price table, two rounds of min-price-path search, round 0's tokens reseeding a sharper table for round 1) as dp_round plus PriceCounts/PriceTable, reusing parse_greedy's hash-chain match finder and the archive's carry reuse for long matches. One deliberate deviation from the archive's own DP, not from its real behavior: the archive's internal price simulation deduplicates a fresh match's distance against the existing rep cache, but the archive's actual decoder always shifts blindly (the rule replay already implements); porting the dedup rule would let the DP pick a Token::Rep slot based on a cache state replay never reaches, corrupting round-trip whenever a fresh match's distance coincides with an already-cached one. dp_round's cache bookkeeping matches replay unconditionally instead, closing that hazard structurally. 11 new unit tests mirror parse_greedy's suite (empty, single byte, below-OPTIMAL_MIN_LEN fallback, all-literals, simple repeat, overlapping run-length, a 200,000-byte carry-path run, alternating rep-cache, cyclic data, structured near-duplicates, zero-byte binary, dense short-distance 3-byte repeats, and a pseudo-random fixture), every one asserting replay(parse_optimal(data)) == data.", "commit": null}
{"id": "it51", "date": "2026-08-23", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the next M1 slice after the LZ parsers (S2-A8, S2-A9) is porting the archive's adaptive range coder (Enc/Dec) into a new src/coder.rs, ahead of the context-mixing entropy models since those models are frequency tables that drive this coder, not the other way around.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported Enc/Dec's behavior (32-bit [low, high] interval in u64 arithmetic, three renormalization cases with carry deferred via a pending-bit counter, byte-oriented bit packer) as Encoder/Decoder, taking caller-supplied [cum_low, cum_high) out of total on every call rather than owning a model. One behavior-preserving deviation: the archive decoder's empty-first-branch if/else-if chain became a shift: bool computed once per loop iteration, same three cases, same bit-for-bit renormalization, no empty block. 9 unit tests cover empty/single-symbol streams, skewed frequencies, a full 256-symbol alphabet, a 5000-symbol xorshift32 pseudo-random fixture, raw-bit round-trip across six widths, an interleaved symbol/raw-bits fixture matching the shape a length/offset model will need, and a truncated-stream decode asserting no panic; every symbol test drives the coder through a small unexported order-0 FreqTable test fixture so round-trips exercise real cumulative-frequency updates.", "commit": null}
{"id": "it52", "date": "2026-08-23", "kind": "patch", "hypothesis": "Neither the codec (M1) nor the benchmark harness (M2) can measure a bits/byte delta yet; the next M1 slice after the range coder (S2-A10) is porting the archive's order-0 adaptive frequency table (Model) into a new src/model.rs, the type the flag/length/offset stages of the entropy models will each instantiate directly, ahead of the larger six-expert Lit literal mixer.", "verdict": "accepted", "train_delta_bpb": null, "val_delta_bpb": null, "corpus": "N/A (no codec-measuring corpus run; unit tests only)", "mechanism": "Ported Model's increment-then-halve update rule and linear cumulative-frequency scan (INC=12, LIM=65536) as model::Model::encode/decode, driving coder::Encoder/Decoder with real data-derived ranges instead of the coder test suite's own minimal stand-in. decode never panics on adversarial decoder state: Decoder::target is mathematically bounded to [0, total), and total always equals the sum of freq by construction, so the cumulative scan always finds a symbol before running past the table regardless of what bytes produced the decoder's value. 8 unit tests cover empty/single-symbol streams, skewed frequencies, a full 256-symbol alphabet, a 5000-symbol pseudo-random fixture, an explicit rescale-triggering fixture (10,000 codes over a 2-symbol alphabet crosses LIM several times), two independent Model instances interleaving on one coder stream (the flag+length shape S2-D2 still needs), and a truncated-stream decode asserting no panic.", "commit": null}
